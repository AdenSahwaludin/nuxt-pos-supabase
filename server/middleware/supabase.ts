// Server middleware for Supabase
export default defineEventHandler(async (event) => {
  // This middleware can be used for server-side Supabase operations
  // For now, it's a pass-through middleware
  // You can add server-side auth checks, logging, etc. here if needed
});
